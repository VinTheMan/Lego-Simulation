using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SelectingGameObject : MonoBehaviour {




    /*
    public InputField text_x ;
    public InputField text_y ;
    public InputField text_z ;

    public void ValueChangeX( string _str ) {

        float x = float.Parse( _str ) ;
        this.transform.position = new Vector3(x, this.transform.position.y, this.transform.position.z) ;

    } // valuechangeX

    public void ValueChangeY( string _str ) {

        float y = float.Parse( _str ) ;
        this.transform.position = new Vector3(this.transform.position.x, y, this.transform.position.z) ;

    } // valuechangeY

    public void ValueChangeZ( string _str ) {

        float z = float.Parse( _str ) ;
        this.transform.position = new Vector3(this.transform.position.x, this.transform.position.y, z) ;

    } // valuechangeZ
    */

    private void Update() {
        
        /*
        if ( this.transform.childCount == 0 ) // if gameobject didn't have child, position xyz = 0 ;
            this.transform.position = new Vector3(0,0,0) ;

        text_x.text = this.transform.position.x.ToString() ;
        text_y.text = this.transform.position.y.ToString() ;
        text_z.text = this.transform.position.z.ToString() ;
        */

    } // Update

}
